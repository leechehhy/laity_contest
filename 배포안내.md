# 인터넷에 공개해서 누구나 접속하게 하기 (Render 배포)

집·출장지 등 어디서든 접속 가능한 `https://...` 주소를 만드는 방법입니다.
개발 지식 없이 **웹 브라우저 클릭만으로** 진행합니다. 처음 한 번만 약 20분 걸립니다.

---

## 먼저 읽어 주세요

무료 요금제만으로도 **자료가 사라지지 않게** 할 수 있습니다.
Supabase(무료·카드 불필요)를 저장소로 붙이면 됩니다. 방법은 **`저장소연결안내.md`** 에 있습니다.

순서는 이렇습니다.

1. 이 문서대로 Render 에 무료로 배포 → 주소 발급
2. `저장소연결안내.md` 대로 Supabase 를 연결 → 자료 보관 해결

---

## 참고 — 무료 요금제 자체의 한계

| 구분 | 무료(Free) | 유료(Starter, 약 $7/월) |
|---|---|---|
| 접속 주소(https) | ○ | ○ |
| 15분간 접속이 없으면 | **잠자기** (다음 접속 시 30초쯤 로딩) | 항상 켜져 있음 |
| 신청 내용·첨부파일 보관 | **재시작하면 사라짐** | 디스크에 안전하게 보관 |

**첨부파일을 받는 사이트라 무료 요금제만으로는 위험합니다.**
접수 기간(8월~9월) 두 달만 Starter 를 쓰고 대회가 끝나면 삭제하면 **총 2만원 안쪽**입니다.

무료로 꼭 진행하셔야 한다면, **매일 관리자 화면에서 엑셀과 첨부파일 ZIP 을 내려받아 두세요.**
(무료 설정 방법은 아래 6단계에 함께 적어 두었습니다.)

---

## 1단계. GitHub 계정 만들기 (3분)

Render 는 파일을 GitHub 에서 가져갑니다. 코드를 다룰 줄 몰라도 됩니다.

1. <https://github.com> 접속 → **Sign up**
2. 이메일·비밀번호·아이디 입력 후 가입 (무료)

## 2단계. 저장소 만들고 파일 올리기 (5분)

1. 로그인 후 오른쪽 위 **＋** → **New repository**
2. **Repository name** 에 `laity-contest` 입력
3. **Private** 선택 (교내용이므로 비공개 권장)
4. **Create repository** 클릭
5. 다음 화면에서 **uploading an existing file** 링크 클릭
6. 압축을 푼 `laity-site` **폴더 안의 내용물 전체**를 드래그해서 올립니다

   > `laity-site` 폴더 자체가 아니라, 그 **안에 있는** `server.js`, `config.json`, `public`, `lib`, `forms` 등을 올려 주세요.

7. 아래 **Commit changes** 클릭

## 3단계. Render 가입 (2분)

1. <https://render.com> 접속 → **Get Started** → **GitHub 로 로그인**
2. GitHub 접근 권한 요청이 뜨면 **Only select repositories** → `laity-contest` 선택 → 승인

## 4단계. 서비스 만들기 (3분)

1. Render 대시보드에서 **New +** → **Web Service**
2. `laity-contest` 저장소 선택 → **Connect**
3. 화면에 `render.yaml` 을 발견했다는 안내가 나오면 그대로 **Apply** 를 누르면 설정이 자동으로 잡힙니다.

   자동으로 잡히지 않으면 아래처럼 직접 입력하세요.

   | 항목 | 값 |
   |---|---|
   | Name | `laity-contest` |
   | Region | `Singapore` |
   | Runtime | `Node` |
   | Build Command | *(비워 둠)* |
   | Start Command | `node server.js` |
   | Instance Type | `Starter` (유료) 또는 `Free` |

4. **Advanced** 를 펼쳐 환경변수(Environment Variables) 2개를 추가합니다.

   | Key | Value |
   |---|---|
   | `DATA_DIR` | `/var/data/db` |
   | `UPLOAD_DIR` | `/var/data/uploads` |

5. 같은 화면에서 **Add Disk** 를 눌러 저장소를 붙입니다. *(Starter 요금제에서만 가능)*

   | 항목 | 값 |
   |---|---|
   | Name | `laity-data` |
   | Mount Path | `/var/data` |
   | Size | `2` GB |

6. **Create Web Service** 클릭

## 5단계. 주소 확인 (2분)

배포가 끝나면(2~3분) 화면 위쪽에 주소가 나옵니다.

```
https://laity-contest.onrender.com
```

이 주소를 교직원에게 공지하시면 됩니다. 관리자 화면은 뒤에 `/admin` 을 붙이면 됩니다.

```
https://laity-contest.onrender.com/admin
```

주소를 바꾸고 싶으면 **Settings → Name** 에서 이름을 고치면 `https://원하는이름.onrender.com` 이 됩니다.
학교 도메인(`laity.gwangju.ac.kr`)을 쓰시려면 **Settings → Custom Domain** 에 등록한 뒤
정보전산원에 DNS 연결(CNAME)을 요청하시면 됩니다.

## 6단계. 배포 직후 꼭 하실 일

1. **관리자 비밀번호 변경** — `config.json` 의 `adminPassword` 를 바꾸세요.
   GitHub 저장소에서 `config.json` 을 열고 연필 아이콘 → 수정 → **Commit changes** 하면
   Render 가 자동으로 다시 배포합니다. (문구·일정 수정도 같은 방법입니다.)
2. **담당자 연락처 변경** — `config.json` 의 `contact` 항목을 실제 정보로.
3. **무료 요금제로 운영하는 경우** — 환경변수와 디스크 없이 배포하고,
   매일 관리자 화면에서 **엑셀 다운로드**와 **첨부파일 전체 ZIP** 을 내려받아 보관하세요.

---

## 자주 묻는 것

**Q. 접속했더니 30초쯤 하얀 화면이 나옵니다.**
무료 요금제는 접속이 없으면 잠들었다가 깨어납니다. 조금 기다리면 열립니다. Starter 요금제에서는 발생하지 않습니다.

**Q. 신청 내용이 사라졌어요.**
무료 요금제에서 디스크 없이 운영하면 재시작 시 초기화됩니다. Starter + 디스크로 바꾸셔야 합니다.

**Q. 개인정보가 교외 서버에 저장되는데 괜찮나요?**
성명·소속·연락처·이메일이 해외 서버(싱가포르)에 저장됩니다. 학교 개인정보 처리방침상 문제가 없는지
정보보안 담당 부서에 한 번 확인하시고, 대회 종료 후에는 Render 대시보드에서 서비스를 **Delete** 해
데이터를 완전히 지워 주세요. 교내 서버에 두는 것이 가장 안전합니다.

**Q. 대회가 끝나면?**
관리자 화면에서 엑셀·첨부파일 ZIP 을 모두 내려받아 보관한 뒤,
Render **Settings → Delete Service** 로 삭제하면 요금이 더 부과되지 않습니다.

---

## 교내 서버에 올리는 경우 (정보전산원 요청용)

- Node.js 18 이상만 있으면 됩니다. **설치할 패키지가 없습니다** (`npm install` 불필요).
- 실행: `node server.js` (기본 3000 포트, `PORT` 환경변수로 변경 가능)
- 데이터 위치: `DATA_DIR`, `UPLOAD_DIR` 환경변수로 지정 (미지정 시 프로그램 폴더 하위 `data/`, `uploads/`)
- 상태 확인 경로: `/healthz`
- 상시 실행: `pm2 start server.js --name laity` 또는 systemd 서비스 등록
- 리버스 프록시(Nginx/Apache)로 443 → 3000 연결, 업로드 크기 제한 `client_max_body_size 60M` 이상 권장
