# Supabase 연결하기 (무료 · 카드 등록 불필요)

Render 무료 요금제는 서버가 재시작되면 신청 내용과 첨부파일이 사라집니다.
Supabase 를 붙이면 **무료 그대로 자료가 안전하게 보관**됩니다. 처음 한 번, 약 10분 걸립니다.

> **중요**
> 여기서 복사하는 `service_role` 키는 **비밀번호와 같습니다.**
> GitHub 에 올리거나 메신저·메일로 남에게 보내지 마세요. Render 설정 화면에만 붙여넣습니다.

---

## 1단계. Supabase 가입 (2분)

1. <https://supabase.com> 접속 → 오른쪽 위 **Start your project**
2. **Continue with GitHub** 으로 로그인 (GitHub 계정 그대로 사용)
3. 카드 등록을 요구하지 않습니다.

## 2단계. 프로젝트 만들기 (3분)

1. **New project** 클릭
2. 아래처럼 입력

   | 항목 | 값 |
   |---|---|
   | Name | `laity-contest` |
   | Database Password | 아무 비밀번호나 (쓸 일 없지만 어딘가 적어 두세요) |
   | Region | `Northeast Asia (Seoul)` |
   | Plan | `Free` |

3. **Create new project** → 준비되는 데 1~2분 걸립니다.

## 3단계. 저장소(버킷) 만들기 (1분)

1. 왼쪽 메뉴에서 **Storage** 클릭
2. **New bucket** 클릭
3. Name 에 **`laity`** 입력 (반드시 이 이름으로)
4. **Public bucket 은 켜지 마세요.** 꺼진 상태(비공개)여야 첨부파일이 외부에 노출되지 않습니다.
5. **Create bucket**

## 4단계. 연결 정보 2개 복사 (2분)

1. 왼쪽 메뉴 맨 아래 **Project Settings**(톱니바퀴) → **API Keys** (또는 **API**)
2. 아래 두 가지를 각각 복사해 둡니다.

   | 이름 | 생김새 |
   |---|---|
   | **Project URL** | `https://abcdefghijk.supabase.co` |
   | **service_role** 키 | `eyJhbGciOi...` 로 시작하는 아주 긴 문자열 |

   > `anon` / `publishable` 키가 아니라 **`service_role`** 키여야 합니다.
   > 가려져 있으면 **Reveal** 을 눌러 표시한 뒤 복사하세요.

## 5단계. Render 에 등록 (2분)

1. Render 대시보드 → `laity-contest` 서비스 클릭
2. 왼쪽 메뉴 **Environment** 클릭
3. **Add Environment Variable** 로 아래 3개를 추가

   | Key | Value |
   |---|---|
   | `SUPABASE_URL` | 4단계에서 복사한 Project URL |
   | `SUPABASE_KEY` | 4단계에서 복사한 service_role 키 |
   | `SUPABASE_BUCKET` | `laity` |

4. **Save, rebuild, and deploy** 클릭 → 2~3분 뒤 다시 배포됩니다.

## 6단계. 확인

1. Render 서비스 화면의 **Logs** 탭을 엽니다.
2. 아래 문구가 보이면 성공입니다.

   ```
   [storage] Supabase 연결 OK  (bucket: laity)
   [storage] 신청 0건 · 질문 0건 불러옴
   ```

3. 사이트에서 시험 삼아 신청서를 하나 넣어 보세요.
4. Supabase 화면의 **Storage → laity** 에 들어가면 `db` 와 `uploads` 폴더가 생겨 있습니다.
5. Render 대시보드에서 **Manual Deploy → Deploy latest commit** 으로 강제로 재시작한 뒤,
   관리자 화면에 방금 넣은 신청서가 **그대로 남아 있으면** 완료입니다.

---

## 이제 어떻게 되나요

| 항목 | 저장 위치 |
|---|---|
| 신청 내용 · 질의응답 | Supabase Storage 의 `db/` 폴더 (JSON) |
| 제안서 · 증빙 첨부파일 | Supabase Storage 의 `uploads/` 폴더 |
| 사이트 화면 · 프로그램 | Render (GitHub 저장소에서 자동 배포) |

- Render 가 재시작되거나 잠들었다 깨어나도 **자료가 사라지지 않습니다.**
- 무료 한도는 저장공간 1GB 입니다. 신청 50건에 각 5MB 씩 잡아도 250MB 라 충분합니다.
- 환경변수를 지우면 다시 로컬 폴더(`data/`, `uploads/`)에 저장하는 방식으로 돌아갑니다.
  담당자 PC 에서 `START.bat` 으로 띄울 때는 환경변수가 없으므로 그대로 로컬에 저장됩니다.

## 대회가 끝나면

1. 관리자 화면에서 **엑셀**과 **첨부파일 전체 ZIP** 을 내려받아 보관
2. Supabase → **Project Settings → General → Delete project**
3. Render → **Settings → Delete Service**

## 자주 생기는 문제

| 증상 | 확인할 것 |
|---|---|
| Logs 에 `Supabase 연결 실패` | 키를 `anon` 으로 잘못 넣지 않았는지, URL 끝에 `/` 가 붙지 않았는지 |
| `bucket not found` | 버킷 이름이 정확히 `laity` 인지, `SUPABASE_BUCKET` 값과 같은지 |
| 첨부파일이 안 열림 | 버킷이 Public 으로 되어 있어도 동작은 합니다. 비공개 권장 |
| Supabase 프로젝트가 일시정지됨 | 무료 프로젝트는 1주일간 아무 접속이 없으면 멈춥니다. 대시보드에서 **Restore** 를 누르면 됩니다 |
